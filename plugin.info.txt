base    ${project.artifactId}
author  Mark C. Prins
email   mprins@users.sf.net
date    ${buildNumber} (${project.version})
name    ${project.name}
desc    ${project.description}. Uses OpenLayers: ${openlayers.checkout.versiontype} ${openlayers.checkout.version}
url     ${project.url}
